{
  "id": "sshd",
  "version": "1.1.0",
  "name": "SSH Server",
  "description": "Adds SSH access to a devcontainer with support for agent forwarding and customizable ports.",
  "options": {
    "authorizedKeys": {
      "type": "string",
      "default": "",
      "description": "Newline-separated SSH public keys."
    },
    "user": {
      "type": "string",
      "default": "automatic",
      "description": "User allowed to log in."
    },
    "sshPort": {
      "type": "string",
      "default": "22",
      "description": "The port the SSH server should listen on."
    }
  },
  "postStartCommand": "/usr/local/share/devcontainer-sshd/start-sshd.sh"
}
